# BERT-Efficiency-Techniques
This repository demonstrates advanced BERT fine-tuning efficiency techniques such as pruning, distributed and parallel training, parameter sharing, layer freezing, quantization, gradient checkpointing, and mixed precision training using the OpenRank dataset (Cars and Hotels reviews).
